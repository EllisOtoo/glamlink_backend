services:
  db:
    image: postgres:16-alpine
    container_name: glamlink-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: glamlink
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # api:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: glamlink-api
  #   depends_on:
  #     db:
  #       condition: service_healthy
  #   environment:
  #     NODE_ENV: production
  #     DATABASE_URL: postgresql://postgres:postgres@db:5432/glamlink?schema=public
  #     PORT: 3000
  #   ports:
  #     - "3000:3000"

volumes:
  pgdata:
